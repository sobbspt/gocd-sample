format_version: 9
pipelines:
  build:
    materials:
      repository:
        git: https://github.com/sobbspt/gocd-sample.git
        branch: master
    stages:
      - test:
          clean_workspace: true
          jobs:
            run-unit-test:
              tasks:
                - exec:
                    command: ./gradlew test
